# Commit Conventions Reference

See `.claude/skills/git-commit/references/commit-conventions.md` for the full commit and branch naming convention. Keep both files in sync — this file exists only because the write-pr skill needs it for issue-number extraction and should not drift from git-commit's version.